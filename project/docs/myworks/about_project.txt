step-2 created frontend using nextjs
    1> installed shadcnui - framework:nextjs, preset: nova, component library: Base UI
        shadcn/ui is a set of beautifully-designed, accessible components and a code distribution platform.
        Works with your favorite frameworks and AI models. Open Source. Open Code.

    2> installed react-hook-form :Performant, flexible and extensible forms with easy-to-use validation.

    3> installed zod 
        Zod is a TypeScript-first validation library.
        Define a schema and parse some data with it. You'll get back a strongly typed, validated result.

    4> installed tanstack/react-query
        TanStack Query (formerly known as React Query) is often described as the missing data-fetching library for web applications,
        but in more technical terms,
        it makes fetching, caching, synchronizing and updating server state in your web applications a breeze.

    5> installed recharts
        The main purpose of this library is to help you to write charts in React applications without any pain. Main principles of Recharts are:
        -Simply deploy with React components.
        -Native SVG support, lightweight with minimal dependencies.
        -Declarative components.

created backend installed dependencies
    1> express: it is a open source web library for nodejs which help to create rest apis and offers inbuild middlewares

    2> ts-node-dev: It restarts target node process when any of required files changes (as standard node-dev) but shares Typescript compilation process between restarts. 
    This significantly increases speed of restarting comparing to node-dev -r ts-node/register ...,
     nodemon -x ts-node ... variations because there is no need to instantiate ts-node compilation each time.

    3> prisma: Prisma is an open-source Object-Relational Mapper (ORM) that acts as a translator between a Node.js/TypeScript application
     and a database. Instead of writing raw SQL queries or complex database commands, 
     developers use Prisma to interact with their data using clean, auto-generated JavaScript or TypeScript methods

    4> pino : pino is a very low-overhead, structured JSON logging library for Node.js applications. 
    It is widely used in the JavaScript ecosystem because it is designed to be significantly faster than older alternatives
     like Winston or Bunyan, often boasting a speed advantage of over 5x

    5> cors: The cors npm package is a Node.js middleware designed for Express (and Connect) applications that simplifies 
    the process of enabling Cross-Origin Resource Sharing (CORS). 
    It automatically sets the necessary HTTP response headers (like Access-Control-Allow-Origin) that instruct web browsers to allow a frontend application running on one domain (e.g., http://localhost:3000)
    to safely request and read resources from a backend server running on a different domain (e.g., http://example.com)

    6> helmet :  is a popular open-source middleware package for Node.js, 
    (primarily used with Express) that helps secure your web applications by automatically setting various HTTP response headers.

    7> bullMQ: BullMQ is a fast, robust, and Redis-based message and job queue library designed for Node.js 
    and other environments like Python, Rust, and Elixir. It is the modern successor to the popular older library called Bull.

    8> ioredis: ioredis is a robust, high-performance, and feature-rich Redis client for Node.js and JavaScript.


step-3 Monorepo Structure:
    directory layout: frontend/, backend/, docs/, and evals/.
    added project/backend/tsconfig.jsonand configured npm run dev, npm run build, and npm run start scripts in 
    project/backend/package.json


    Environment & Docker Config:
        Created project/backend/.env.example
        containing: DATABASE_URL, REDIS_URL, GEMINI_API_KEY, GROQ_API_KEY, OPENROUTER_API_KEY, JWT_SECRET, PORT
        Created project/backend/Dockerfile
        stub.
        Configured .gitignore files to ignore node_modules, .env, dist, .next.

    PostgreSQL Database:
        Connected to the local PostgreSQL instance on port 5432 and ensured the stackprep database is active and reachable.\

    Prisma Schema Configuration (
    schema.prisma
    ):

    Defined all required models:
        User
        Resume
        Interview
        Question
        Answer
        Evaluation
        UsageLimit
        AIUsage
        PromptVersion
        EvalRun
        EvalResult
        Configured all relational foreign keys and cascade deletions (onDelete: Cascade on User → Resume, User → Interview, Interview → Question, Question → Answer, Answer → Evaluation, EvalRun → EvalResult, etc.).
        Added indexes on Interview.userId, Question.interviewId, Answer.questionId, AIUsage.userId, and AIUsage.createdAt.

    Prisma Migrations & Client Generation:
        Applied the initial migration 20260924160124_init cleanly to PostgreSQL via npx prisma migrate dev --name init.
        Generated the Prisma Client (@prisma/client).
        Verified that all 11 tables exist in the stackprep database.

    Singleton Prisma Client (prisma.ts):
        Implemented the singleton Prisma Client instance with environment-aware logging.
        Verified database connectivity and queries through the singleton client.

    Tracking & Git:
        Updated the checklist in 
        nextsteps.md
        Committed the changes with Git.


step-4 Authentication Service & Cryptography:
    Password Hashing: Implemented password hashing and validation with bcrypt (10 salt rounds) in 
    auth.service.ts
    
    Token Generation: Configured short-lived Access Tokens (1h) and Refresh Tokens (7d) with jsonwebtoken.
    Validation: Added Zod schemas for input validation in 
    auth.validator.ts
    
    User Provisioning: signup creates the User and provisions initial UsageLimit records.

    Authentication Middleware & Controller:
        Middleware (auth.middleware.ts): Verifies access tokens from either the Authorization: Bearer <token> header or accessToken cookie and attaches req.user. 
            Returns 401 Unauthorized on invalid or missing tokens.
        Controller (auth.controller.ts): Implemented handlers for signup, login, logout (clearing HTTP-only cookies), and getMe.

    Endpoints & Routing:
        POST /api/auth/signup — Registers user, hashes password, sets auth cookies, and returns JWT tokens.
        POST /api/auth/login — Verifies password hash, sets auth cookies, and returns JWT tokens.
        POST /api/auth/logout — Clears authentication cookies.
        GET /api/me & GET /api/auth/me — Protected current-user endpoint.

    Object-Level Authorization Helper:
        Implemented assertOwnsResource(userId, resourceUserId) in 
        authorization.service.ts
        Throws a ForbiddenError (403 Forbidden) whenever a user attempts to access or mutate a resource owned by another user.

    Frontend Route Guard:
        Configured Next.js Route Protection in 
        middleware.ts
        to guard /dashboard and /interview routes against unauthenticated access.
        Added placeholder pages for /dashboard, /login, and /signup.

    Automated Verification:
        Executed full integration test (step4-auth.test.ts):
            Protected route without token returns 401 Unauthorized
            User signup and login return valid JWTs
            GET /api/me returns authentic user profile
            Cross-user access check via assertOwnsResource returns 403 Forbidden
            logout succeeds and clears cookies
            Both backend and frontend builds pass TypeScript checks cleanly.
